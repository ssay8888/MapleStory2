syntax = "proto3";
package Protocol;

enum kLoginMessage
{
	kLoginSuccess = 0;
	kIdError = 1;
	kPasswordError = 2;
	kAlreadyConnected = 3;
}

enum kCreateMessage
{
	kCreateSuccess = 0;
	kDuplication = 1;
	kCreateFailed = 2;
}

enum kCharacterSelectMessage
{
	kSelectSuccess = 0;
	kSelectFailed = 1;
}