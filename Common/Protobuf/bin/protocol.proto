syntax = "proto3";
package Protocol;

import "enum.proto";
import "struct.proto";

message ClientLogin
{
	string id = 1;
	string pw = 2;
	string auth = 3;
}

message ServerLogin
{
	kLoginMessage result = 1;
}
